import 'dotenv/config'; //닷이엔브이 설정을 가져온다

export const SERVER_PORT = process.env.SERVER_PORT; //env에서 서버포트정보가져오고 serverport로 내보낸다
export const ACCESS_TOKEN_SECRET = process.env.ACCESS_TOKEN_SECRET; //env에서 엑세스토큰정보가져오고 accestokensecret으로 내보낸다
